import React from "react";

// GET db info and find which place IS sactive

// Pass db info down to child components:
// places.text to TextBox
// conditionally render actions based on active places.id
// conditionally render Alert if we are in a fighting state.

const Place = () => {
  return <div>Place</div>;
};

export default Place;
